# This is intended to be included from inside a Makefile

# These are set if they're not already specified in environment variables.
PACKAGE_VERSION ?= local
PACKAGE_IMG ?= crossplane/app-wordpress:$(PACKAGE_VERSION)
